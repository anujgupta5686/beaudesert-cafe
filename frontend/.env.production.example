# Copy to frontend/.env.production on EC2 BEFORE npm run build
# Vite bakes VITE_* at build time — rebuild after any change.

# Must match your API subdomain (HTTPS after Certbot)
VITE_API_URL=https://api.YOUR_DOMAIN.com/api

# Optional
VITE_ADMIN_EMAIL=beaudesertcafe@gmail.com
